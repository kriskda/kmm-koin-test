pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "kmm-koin-test"
include(":androidApp")
include(":config")
include(":domain")
include(":data")
